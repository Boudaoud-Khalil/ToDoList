export class List {
    constructor(public listtitle: string, public listtext: string, private isfavorite: boolean = false) {}
}
export class Lists {
    constructor(public listtitle: string, public listtext: string, private isfavorite: boolean = false) {}
}